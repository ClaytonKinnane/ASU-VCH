# SECURITY REQUIRED PASSWORD CHANGE V1 — TEST REPORT

Статус: Passed locally
Дата: 2026-07-25
Ветка: `feature/required-password-change`
Проверенный код реализации: `a9b6f3f7636e1e5fec60b0e6b8c22ea745ca442a`

## 1. Область тестирования

Проверен инкремент обязательной самостоятельной смены временного пароля:

- перенаправление после успешного входа при `must_change_password = 1`;
- центральная защита административных маршрутов;
- форма обязательной смены пароля;
- проверка текущего пароля;
- политика нового пароля;
- запрет повторного использования текущего пароля;
- снятие `must_change_password` и `is_temporary` после успеха;
- регенерация сессии и CSRF-контекста;
- принуждение уже активной сессии после установки флага администратором;
- desktop/mobile представление.

## 2. Среда

- Windows;
- Open Server Panel 6.5.1;
- Apache;
- PHP 8.5.4 CLI;
- MySQL 8.4;
- PowerShell 5.1;
- локальный домен `https://asu-vch.local`;
- репозиторий `C:\Project\ASU-VCH`;
- каталог развертывания `C:\OSPanel\home\asu-vch.local`.

## 3. CLI и автоматизированные проверки

### 3.1 PHP syntax

Результат:

- проверено PHP-файлов: 34;
- синтаксических ошибок: 0.

Статус: PASS.

### 3.2 Развертывание

Результат:

- Open Server Panel 6.5.1 обнаружен;
- Apache, PHP 8.5 и MySQL 8.4 обнаружены;
- локальный `config/local.php` сохранен и восстановлен;
- скопировано файлов: 44;
- сайт развернут в `C:\OSPanel\home\asu-vch.local`.

Статус: PASS.

### 3.3 Установка и миграции

Результат:

- база данных: `asu_vch`;
- применено миграций: 3;
- новых миграций нет;
- пользователей до функциональных проверок: 3;
- первичная регистрация отключена.

Статус: PASS.

### 3.4 RBAC check

Результат:

- `OK migration 002`;
- `OK system roles: 4`;
- `OK system permissions: 18`;
- `OK active owners: 1`.

Статус: PASS.

### 3.5 Approval check

Результат:

- `OK migration 003`;
- `OK approval columns: 5`;
- `OK approved users: 3`;
- `OK active approved owners: 1`.

Статус: PASS.

### 3.6 Required password change service check

Результат:

- `OK wrong current password rejected`;
- `OK weak password rejected`;
- `OK confirmation mismatch rejected`;
- `OK current password reuse rejected`;
- `OK password hash changed`;
- `OK temporary flags cleared`.

Тестовая учетная запись удаляется проверочным скриптом перед завершением.

Статус: PASS.

### 3.7 Local smoke test

Результат:

- `/` — HTTP 200;
- `/health.php` — HTTP 200;
- `/admin/` без авторизации — HTTP 302.

Статус: PASS.

## 4. Ручные функциональные проверки

### 4.1 Подготовка учетной записи

Для `User_02` администратором установлены:

- `is_temporary = 1`;
- `must_change_password = 1`.

Статус: PASS.

### 4.2 Перенаправление после входа

После успешной проверки текущего пароля пользователь перенаправлен на:

`/account/change-password.php`.

Обычная административная панель до смены пароля не открывается.

Статус: PASS.

### 4.3 Форма обязательной смены пароля

Проверено:

- отображается имя и логин текущего пользователя;
- присутствуют поля текущего, нового и подтверждения нового пароля;
- присутствуют кнопки `Показать / Скрыть`;
- присутствует выход из системы;
- парольные поля после ошибок пустые;
- парольные значения не сохраняются в URL или HTML.

Статус: PASS.

### 4.4 Неверный текущий пароль

Неверный текущий пароль отклонен сообщением:

`Текущий пароль указан неверно.`

Статус: PASS.

### 4.5 Слабый пароль

Пароль, не соответствующий политике 10–128 символов, минимум одна буква и одна цифра, отклонен.

Статус: PASS.

### 4.6 Несовпадающее подтверждение

Несовпадающее подтверждение отклонено сообщением:

`Подтверждение пароля не совпадает.`

Статус: PASS.

### 4.7 Повторное использование текущего пароля

Текущий пароль не принят как новый. Отображено сообщение:

`Новый пароль должен отличаться от текущего.`

Статус: PASS.

### 4.8 Успешная смена пароля

После корректной смены:

- пользователь перенаправлен в `/admin/`;
- отображено сообщение об успешной смене;
- `must_change_password = 0`;
- `is_temporary = 0`;
- карточка пользователя показывает `Нет` для обоих флагов.

Статус: PASS.

### 4.9 Старый и новый пароль

Проверено:

- старый пароль после смены отклоняется;
- новый пароль принимается;
- повторная обязательная смена после входа с новым паролем не запрашивается.

Статус: PASS.

### 4.10 Защита прямых административных маршрутов

Пока `must_change_password = 1`, ручные переходы на:

- `/admin/`;
- `/admin/users.php`;
- `/admin/settings.php`;
- `/admin/content.php`

возвращают пользователя на `/account/change-password.php`.

Статус: PASS.

### 4.11 Выход и повторный вход

Выход со страницы обязательной смены пароля работает. При повторном входе до завершения смены пользователь снова направляется на обязательную форму.

Статус: PASS.

### 4.12 Принуждение активной сессии

После установки `must_change_password = 1` администратором уже авторизованный пользователь при следующем HTTP-запросе перенаправляется на обязательную смену пароля без повторного входа.

Статус: PASS.

### 4.13 CSRF

После подмены `csrf_token` на `invalid` сервер вернул HTTP 419 и сообщение:

`Недействительный CSRF-токен.`

После повторного открытия страницы сформирован новый корректный токен.

Статус: PASS.

## 5. Визуальная и адаптивная проверка

### 5.1 Desktop

Проверено:

- темно-синяя тема;
- стеклянные плитки;
- бирюзовые акценты;
- центральная карточка;
- кнопки и поля не выходят за границы;
- ошибки читаемы;
- кнопки показа пароля работают.

Статус: PASS.

### 5.2 Mobile 390 px

Проверено:

- горизонтального переполнения нет;
- заголовок и описание корректно переносятся;
- поля и кнопки помещаются в карточку;
- кнопка выхода доступна;
- основная кнопка доступна после вертикальной прокрутки.

Статус: PASS.

### 5.3 Mobile 360 px

Проверено:

- горизонтального переполнения нет;
- заголовок корректно переносится;
- все три поля доступны;
- кнопки показа пароля находятся внутри полей;
- основная кнопка полностью отображается после вертикальной прокрутки.

Статус: PASS.

Вертикальная прокрутка на мобильной высоте является ожидаемым поведением.

## 6. Git-состояние перед закрытием тестирования

- HEAD локальной ветки: `a9b6f3f`;
- локальная ветка синхронизирована с `origin/feature/required-password-change`;
- рабочее дерево чистое.

## 7. Ограничения текущего инкремента

Не входят в текущий объем:

- добровольная смена пароля при снятом флаге;
- административный сброс пароля;
- восстановление забытого пароля;
- история и срок действия паролей;
- завершение других пользовательских сессий;
- MFA;
- уведомления по email.

GitHub CI пока отсутствует. Источником результатов являются локальные CLI-проверки, функциональная и визуальная приемка.

## 8. Итог

Security Required Password Change v1 реализован и локально проверен. Все обязательные сценарии завершены со статусом PASS. Блокирующих дефектов в принятом объеме не обнаружено.

Рекомендация: создать PR из `feature/required-password-change` в `main`, выполнить финальный review и ожидать отдельного разрешения владельца проекта на merge.
