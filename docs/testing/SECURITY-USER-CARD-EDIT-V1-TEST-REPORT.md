# SECURITY USER CARD & EDIT V1 — TEST REPORT

Статус: Passed locally with documented limitations
Дата: 2026-07-25
Ветка: `feature/initial-site`
Проверенный код интерфейса: `c7770c14c040bdb855994c403fb630e83f5bf616`

## 1. Область тестирования

Проверены принятые изменения:

- компактный список пользователей;
- карточка пользователя;
- редактирование основных данных;
- назначение ролей;
- блокировка и разблокировка;
- подтверждение пользователя из карточки;
- отображение аудита создания и подтверждения;
- ограниченный просмотр для viewer/operator;
- адаптивность desktop/mobile;
- отображение подтверждающего в поле `Подтверждение` со ссылкой на его карточку;
- исправления autofill и мобильной горизонтальной прокрутки сводных плиток.

## 2. Среда

- Windows;
- Open Server Panel 6.5.1;
- Apache;
- PHP 8.5.4 CLI;
- MySQL 8.4;
- PowerShell 5.1;
- локальный домен `https://asu-vch.local`;
- репозиторий `C:\Project\ASU-VCH`;
- каталог развертывания `C:\OSPanel\home\asu-vch.local`.

## 3. Автоматизированные и CLI-проверки

### 3.1 PHP syntax

Результат на коде `c7770c1`:

- проверено PHP-файлов: 31;
- синтаксических ошибок: 0.

Статус: PASS.

### 3.2 Установка и миграции

Результат:

- база данных: `asu_vch`;
- применено миграций: 3;
- новых миграций нет;
- первичная регистрация отключена.

Статус: PASS.

### 3.3 RBAC check

Результат:

- `OK migration 002`;
- `OK system roles: 4`;
- `OK system permissions: 18`;
- `OK active owners: 1`.

Статус: PASS.

### 3.4 Approval check

Результат:

- `OK migration 003`;
- `OK approval columns: 5`;
- `OK approved users`;
- `OK active approved owners: 1`.

Статус: PASS.

### 3.5 Local smoke test

Результат:

- `/` — HTTP 200;
- `/health.php` — HTTP 200;
- `/admin/` без авторизации — HTTP 302;
- smoke test завершен успешно с локальным параметром сертификата.

Статус: PASS.

После smoke test вносились только изменения представления карточки и документации. Серверная модель, база, вход, RBAC и маршруты smoke test не изменялись.

## 4. Ручные функциональные проверки

### 4.1 Компактный список

Проверено:

- таблица содержит только `Пользователь`, `Логин`, `Статус`, `Последний вход`;
- имя пользователя открывает карточку;
- поиск и фильтр работают;
- счетчики корректно отражают active/pending/blocked;
- pending-пользователь отображается со статусом `Ожидает подтверждения`;
- до первого входа отображается `Нет данных`.

Статус: PASS.

### 4.2 Карточка и аудит

Проверено:

- основные сведения;
- роли;
- статус подтверждения;
- признаки временной учетной записи и смены пароля;
- создатель, дата и основание создания;
- подтвердивший и дата подтверждения;
- последний вход и дата изменения.

Статус: PASS.

### 4.3 Редактирование

Проверено:

- изменение отображаемого имени;
- изменение email;
- изменение флагов `is_temporary` и `must_change_password`;
- сохранение после обновления страницы;
- возврат тестовых значений в согласованное состояние.

Статус: PASS.

### 4.4 Валидация уникальности

Проверено:

- существующий логин отклоняется;
- существующий email отклоняется;
- ошибки отображаются возле соответствующих полей;
- PHP exception не возникает.

Статус: PASS.

### 4.5 Роли

Проверено:

- добавление обычной роли;
- отображение нескольких ролей;
- возврат исходного набора ролей.

Статус: PASS.

### 4.6 Блокировка и разблокировка

Проверено:

- статус меняется на `Заблокирован`;
- вход заблокированного пользователя отклоняется;
- после разблокировки статус возвращается в `Активен`;
- после разблокировки вход снова разрешен.

Статус: PASS.

### 4.7 Создание и подтверждение

Проверено:

- новый пользователь создается как pending и inactive;
- вход до подтверждения запрещен;
- карточка pending-пользователя показывает создателя, дату и основание;
- подтверждение выполняется из карточки;
- после подтверждения пользователь активен;
- после подтверждения вход разрешен;
- счетчики списка обновляются корректно.

Статус: PASS.

### 4.8 Отображение подтверждающего

Проверено:

- статус и субъект действия находятся в одном поле `Подтверждение`;
- отдельная плитка `Кем подтвержден` отсутствует;
- выводится только отображаемое имя;
- имя является ссылкой на карточку подтверждающего;
- логин подтверждающего в этом поле не выводится;
- pending-пользователь не получает пустую дополнительную строку.

Статус: PASS.

### 4.9 Ограниченный просмотр

Проверено под viewer:

- список пользователей доступен;
- реальный последний вход заменен на `Недоступно`;
- карточка содержит только имя, логин, статус и роли;
- email, аудит и формы редактирования не отображаются.

Статус: PASS.

### 4.10 Desktop/mobile и тема

Проверено:

- desktop-компоновка списка и карточки;
- мобильная компоновка в одну колонку;
- карточное представление таблицы на узком экране;
- формы и кнопки не выходят за границы;
- autofill формы входа сохраняет темную тему;
- горизонтальная прокрутка сводных плиток оформлена в цветах темы;
- парольные кнопки остаются корректными.

Статус: PASS.

## 5. Ограничения и отложенные проверки

Не выполнялся вручную разрушительный сценарий блокировки или снятия роли у последнего активного владельца. Защитные проверки реализованы в сервисах, но ручной сценарий отложен до подготовки резервной копии и процедуры восстановления.

GitHub CI отсутствует. Источником результатов являются локальные CLI-проверки и ручная визуальная/функциональная приемка.

Не входят в текущий инкремент:

- обязательный change-password flow после входа;
- операция отклонения пользователя;
- `rejected_by`, `rejected_at`, основание отклонения;
- архивирование и восстановление;
- история изменений пользователя.

## 6. Итог

Принятый объем Security User Card & Edit v1 реализован и локально проверен. Блокирующих дефектов в принятом объеме не обнаружено.

Рекомендация: разрешить финальный review PR и после отдельного подтверждения выполнить merge PR №1 в `main`.
